---
name: pm-vs-ai-ssot
description: PM VS AI 单一事实源知识库系统。当项目由"人（PM）用提示词调度 AI 执行者（Claude/Codex/其他 agent）开发"时，建立任务文档 + 主索引 + 提示词归档 + 收工交接 + 事前检查清单的完整留痕体系，保证每项工作可追溯、知识不随对话丢失。用于新项目初始化或给现有项目补文档系统。触发词：文档系统、知识库、任务留痕、单一事实源、PM 调度、SSOT。
---

# PM VS AI 单一事实源知识库系统

当开发由"人（PM）通过提示词调度 AI 执行者干活"主导时，最大的风险不是代码写错，而是**工作过程不可追溯、知识沉淀在对话里、AI 换一个会话就失忆**。本 Skill 提供一套经过实战打磨的文档留痕体系，让项目变成"单一事实源知识库"。

## 这套系统解决什么问题

| 痛点 | 本系统的解法 |
|---|---|
| 上个月做的改动，今天说不清改了什么、为什么改 | 每项工作 = 唯一编号文档 + 主索引，可追溯 |
| 每份任务文档都写了，但没人知道它存在 | 主索引每次任务后必更新，索引滞后 = 知识丢失第一步 |
| 给 AI 的提示词是"一次性消耗品"，做完就丢 | 提示词**下发前**归档为 `T<NNN>-prompt.md` |
| 收工后第二天 AI 开机失忆，交接靠口头 | 每日收工交接存档 + 开机先读交接文档 |
| 文档系统记录了所有警告但无人行动 | 事前检查清单（preflight），开机逐项过，❌ 先修再继续 |

## 核心原则（单一事实源五条）

1. **代码 / 实际状态 > 文档**：文档是快照，代码是真相。文档与代码冲突时以代码为准，并回修文档
2. **索引永远最新**：每次任务后必须更新主索引；这是知识库的生命线
3. **一切工作留痕**：任务报告、提示词、决策、交接，全部落盘，不留在对话里
4. **事前检查 > 事后记录**：主动跑检查清单，而不是被动收记录
5. **诚实记录**：不捏造数据、不得兜底、不确定就停下问；失败就记失败，阻断就记阻断

## 目录结构（在项目中初始化）

```
<项目根>/
├── docs/
│   ├── index.md                    # 主索引（SSOT 入口，时间戳 + 任务表 + 模块速查）
│   ├── preflight-checklist.md      # 开机检查清单
│   ├── tasks/
│   │   ├── index.md                # 任务表格（编号现状 + 最近任务）
│   │   ├── AGENTS.md               # 文档系统子规范（编号规则 + 写作纪律 + 索引维护）
│   │   ├── T<NNN>-<slug>.md        # 任务报告（每任务一份）
│   │   └── T<NNN>-prompt.md        # 提示词归档（每任务一份）
│   ├── conversations/              # 会话记录（可选）
│   │   └── index.md
│   ├── breakpoints/                # 任务级断点（可选）
│   │   └── index.md
│   └── handovers/                  # 每日收工交接存档
│       ├── index.md
│       └── <项目名><M月D日>断点续传.md
├── .claude/
│   └── memory/                     # 长期记忆（见 §记忆系统）
│       ├── MEMORY.md               # 记忆索引
│       └── <name>.md               # 每条记忆一个文件
```

## 核心工作流（任务全生命周期，8 步）

1. **立项**：PM 写提示词，用"提示词分界线"块包裹（`从这里开始复制` / `到这里结束复制`），**一次只发一个任务**
2. **归档提示词**：提示词原文立即存档为 `docs/tasks/T<NNN>-prompt.md`——**下发前就归档**，防止任务做完忘归档
3. **下发执行**：提示词给 AI 执行者；执行者遵守"不得兜底、不捏造、不确定就问"
4. **执行报告**：执行者按模板回报（改了哪些文件、测试结果、发现的问题）
5. **验收**：PM 独立核验——看实际代码 / git / 数据，不盲信报告；发现兜底或造假直接打回
6. **写任务报告**：`docs/tasks/T<NNN>-<slug>.md`
7. **更新索引**：`docs/tasks/index.md` 加行 + `docs/index.md` 顶部"最后更新"时间戳
8. **收工交接**：每日收工写断点续传 → `docs/handovers/`；次日开机先读最新交接

## 文档规范

### 任务报告 `T<NNN>-<slug>.md`
- 固定结构：任务一句话 / 生成时间 / 拍板对象 / 不做（显式排除）/ 文档定位 / 核心内容 / 关键表 / 风险与开放点 / 验收清单(checkbox) / 关联任务 / 编号备注
- 诚实记录：缺数据标"未抓到 / 待补"，禁止用相邻字段填空
- 代码片段不超 20 行；超大 jsonl/xlsx 引用外部文件，不嵌入
- 编号：`T<NNN>-<short-slug>.md`，每个编号唯一；日期不写进文件名

### 提示词归档 `T<NNN>-prompt.md`
- 就是下发给执行者的原文，头部加：状态 / 归档说明 / 依据文档 / 编号
- 用"提示词分界线"标记起止，方便复制
- **缺提示词归档 = 任务未闭环**（最高频的文档系统失守点）

### 主索引 `docs/index.md`
- 顶部一行 `> 最后更新: YYYY-MM-DD (T<NNN> <简述>)`——一眼看到最新状态
- 任务表：编号 / 标题 / 涉及模块 / 状态 / 日期
- 模块速查表：模块 → 核心文件 → 相关任务
- 每次任务前先查索引，确认是否有同主题已有文档

### 每日收工交接 `docs/handovers/`
- 文件名用【收工日期】，不用"明天开机"日期
- 内容：今日成果 / Git 状态 / 已知问题待办表（含严重度）/ 明日开机流程 / 环境速查
- 醒目位置（如桌面）只留最新一份，历史份全部入库
- 交接文件本身也要进索引

### 开机检查清单 `docs/preflight-checklist.md`
- 分类：硬约束已确认 / 文档系统齐全 / 代码健康（git status + 测试）/ 服务状态 / 数据质量 / 上轮已知限制
- 任何 ❌ 项先修再继续，不得跳过；⚠️ 已知基线可记录后继续

## 记忆系统（跨会话不丢失）

在 `.claude/memory/` 下，**每条记忆一个 markdown 文件 + 一个 `MEMORY.md` 索引**：

- **type 分类**：
  - `user`：用户是谁（角色 / 领域经验 / 偏好）
  - `feedback`：协作反馈（教训 / 拍板 / 确认过的方法），附 **Why** 和 **How to apply**
  - `project`：项目事实与约束（不可从代码/git 推导的）
  - `reference`：外部资源指针（URL / 看板 / 票据）
- 每条带 frontmatter（`name` / `description` / `metadata.type`），正文用 `[[其他记忆名]]` 互相链接
- **只记非显然的东西**：代码结构、git 历史、文档已有的，不重复记
- 关键铁律（如不得兜底）用 ⭐ 前缀 + 最高优先级标记
- 索引 `MEMORY.md` 每行一条，是每次会话加载的目录

## 启动流程（新会话 / 新一天）

1. **通读**：最新交接文档 → 主索引 → 关键记忆（⭐ 高优先级铁律必读）
2. **过 preflight 清单**：逐项勾选，❌ 先修再继续
3. **明确工作模式**：谁是 PM（调度/验收/写文档）、谁是执行者（干活/报告）
4. **挑待办**：从交接文档的"已知问题待办表"按优先级继续

## 纪律红线（写进项目 AGENTS.md §0）

> 本 skill 纪律是项目「不得兜底」铁律（AGENTS.md §0 + memory prompt-discipline-reiterate）在协作流程域的落地，与派单硬约束一致：错误查根源→修正→重构，不猜不编不静默。



- ❌ **不捏造数据**：抓不到就标"待补"，绝不凭想象填空
- ❌ **不得兜底**：错误必须查根源 → 修正 → 重构；不允许静默降级、fallback 填值、吞异常
- ❌ **不确定就停下问**：宁可多问一轮，不可拍脑袋决策
- ❌ **批量删 / 批量写前列清单**，等用户逐项确认
- ❌ **对外发布前确认**：开源 / 推送 / 写外部系统前必须确认
- ✅ **中文交互**（如项目语言为中文）

## 模板

全部模板见 `references/templates/`：

| 模板 | 用途 |
|---|---|
| `task-report-template.md` | 任务报告骨架 |
| `prompt-archive-template.md` | 提示词归档骨架（含分界线） |
| `handover-template.md` | 每日收工交接骨架 |
| `index-template.md` | 主索引骨架 |
| `preflight-checklist-template.md` | 开机检查清单骨架 |
| `memory-template.md` | 记忆条目骨架 |

## 实战来源

本系统提炼自一个"PM + 多个 AI 执行者并行开发"的长期实战项目，经历 100+ 项任务迭代打磨。曾因"提示词未归档""索引滞后""只看 diff 不查全仓库"等真实事故不断强化，最终沉淀为这套规则。开源时已剥离全部项目业务信息，只保留通用的方法论。
